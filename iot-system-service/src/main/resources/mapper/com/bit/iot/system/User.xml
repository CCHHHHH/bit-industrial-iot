<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit.iot.system.dao.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.bit.iot.system.model.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="phone_number" property="phoneNumber" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="name_cn" property="nameCn" />
        <result column="email" property="email" />
        <result column="status" property="status" />
    </resultMap>

    <!-- 通用查询结果列（包含密码） -->
    <sql id="Base_Column_List">
        id, username, password, phone_number, create_time, update_time, name_cn, email, status
    </sql>
    
    <!-- 通用查询结果列（不包含密码） -->
    <sql id="User_Response_Column_List">
        id, username, phone_number, name_cn, email, status, create_time, update_time
    </sql>

    <!-- 用户响应 DTO 结果映射 (不包含密码) -->
    <resultMap id="UserResponseDtoResultMap" type="com.bit.iot.system.model.dto.UserResponseDto">
        <id column="user_id" property="id" />
        <result column="username" property="username" />
        <result column="phone_number" property="phoneNumber" />
        <result column="name_cn" property="nameCn" />
        <result column="email" property="email" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <collection property="roles" ofType="com.bit.iot.system.model.dto.RoleDto" javaType="java.util.List">
            <id column="role_id" property="id" />
            <result column="role_name" property="roleName" />
            <result column="role_create_time" property="createTime" />
            <result column="role_update_time" property="updateTime" />
        </collection>
    </resultMap>

    <!-- 查询用户列表（包含角色信息，不包含密码） -->
    <select id="selectUserListWithRoles" resultMap="UserResponseDtoResultMap">
        SELECT 
            u.id as user_id,
            u.username,
            u.phone_number,
            u.name_cn,
            u.email,
            u.status,
            u.create_time,
            u.update_time,
            r.id as role_id,
            r.role_name,
            r.create_time as role_create_time,
            r.update_time as role_update_time
        FROM user u
        LEFT JOIN user_role ur ON u.id = ur.user_id
        LEFT JOIN role r ON ur.role_id = r.id
        WHERE 1=1
        <if test="username != null and username != ''">
            AND u.username LIKE CONCAT('%', #{username}, '%')
        </if>
        ORDER BY u.create_time DESC
    </select>

</mapper>